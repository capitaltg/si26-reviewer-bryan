DATABASE_URL=postgres://postgres:dev@localhost:5434/agentic_review
AUTH_SECRET=generate-with-npx-auth-secret
AUTH_KEYCLOAK_ID=agentic-review
AUTH_KEYCLOAK_SECRET=from-keycloak-admin
AUTH_KEYCLOAK_ISSUER=https://getgaleo.com/realms/REALM_NAME
BLOB_READ_WRITE_TOKEN=from-vercel-blob-store

# Dev-only login (Credentials provider). Set to true for local demos only — NEVER in production.
AUTH_DEV_LOGIN=false
